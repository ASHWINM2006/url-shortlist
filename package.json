{
  "name": "linksnap-root",
  "version": "1.0.0",
  "scripts": {
    "start": "node url-shortener/backend/src/server.js",
    "build": "cd url-shortener/backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
